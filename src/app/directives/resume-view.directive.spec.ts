import { ResumeViewDirective } from './resume-view.directive';

describe('ResumeViewDirective', () => {
  it('should create an instance', () => {
    const directive = new ResumeViewDirective();
    expect(directive).toBeTruthy();
  });
});
